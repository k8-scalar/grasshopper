apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ts-policy-dns
  namespace: test
spec:
  podSelector:
    matchLabels:
      app: teastore
  policyTypes:
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          role: admin
