apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: app-a-deny-all
  namespace: test-thesis
spec:
  podSelector:
    matchLabels:
      app: app-a
  policyTypes:
    - Ingress
    - Egress