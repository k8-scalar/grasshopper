apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: app-c-deny-all
  namespace: test-thesis
spec:
  podSelector:
    matchLabels:
      app: app-c
  policyTypes:
    - Ingress
    - Egress