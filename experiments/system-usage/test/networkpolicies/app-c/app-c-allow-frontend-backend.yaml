apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: app-c-allow-frontend-backend
  namespace: test-thesis
spec:
  podSelector:
    matchLabels:
      app: app-c
      role: frontend
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: app-c
              role: backend
      ports:
        - protocol: TCP
          port: 80 
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: app-c
              role: backend
      ports:
        - protocol: TCP
          port: 80 
  policyTypes:
    - Ingress
    - Egress
