apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: app-b-deny-all
  namespace: test-thesis
spec:
  podSelector:
    matchLabels:
      app: app-b
  policyTypes:
    - Ingress
    - Egress