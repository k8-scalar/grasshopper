apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: app-b-allow-backend-database
  namespace: test-thesis
spec:
  podSelector:
    matchLabels:
      app: app-b
      role: backend
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: app-b
              role: database
      ports:
        - protocol: TCP
          port: 80 
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: app-b
              role: database
      ports:
        - protocol: TCP
          port: 80 
  policyTypes:
    - Ingress
    - Egress
